import {requireUser,logActivity,json,sb} from './_auth.mjs';
export default async function handler(req,res){try{const u=await requireUser(req);if(req.method==='POST'){const b=req.body||{};await logActivity(u.c,u,String(b.action||'ui_action'),{tool:b.tool||'',entityType:b.entityType||'',entityId:b.entityId||'',metadata:b.metadata||{}});return json(res,200,{ok:true})}if(req.method==='GET'){const q=u.role==='admin'?'user_activity_logs?select=*&order=created_at.desc&limit=250':`user_activity_logs?select=*&user_id=eq.${encodeURIComponent(u.id)}&order=created_at.desc&limit=100`;const rows=await sb(u.c,q);return json(res,200,{activity:rows||[]})}res.setHeader('Allow','GET, POST');return json(res,405,{error:'Method not allowed.'})}catch(e){return json(res,e.status||500,{error:e.message})}}
