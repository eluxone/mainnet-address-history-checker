'use strict';
(()=>{const post=(action,tool='navigation',metadata={})=>fetch('/api/activity',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({action,tool,metadata})}).catch(()=>{});async function init(){try{const r=await fetch('/api/me',{credentials:'same-origin',cache:'no-store'});if(!r.ok)return;const d=await r.json(),u=d.user;if(!u)return;if(u.role!=='admin')document.querySelectorAll('a[href="/admin.html"]').forEach(x=>x.remove());const actions=document.querySelector('.crypto-nav-actions');if(actions){const box=document.createElement('div');box.className='crypto-user-box';box.innerHTML=`<span class="crypto-user-name">${escapeHtml(u.displayName||u.username)}</span><span class="crypto-user-role">${escapeHtml(u.role)}</span>${u.role==='admin'?'<a class="crypto-user-admin" href="/admin.html">Admin</a>':''}<button class="crypto-user-logout" type="button">Log out</button>`;actions.prepend(box);box.querySelector('.crypto-user-logout').onclick=async()=>{await post('logout','auth');await fetch('/api/login',{method:'DELETE',credentials:'same-origin'}).catch(()=>{});location.replace('/login.html')}}post('page_view','navigation',{path:location.pathname,title:document.title});document.addEventListener('click',e=>{const b=e.target.closest('button,a');if(!b)return;const text=(b.textContent||'').trim().slice(0,80);if(!text||/password|seed|phrase|private key/i.test(text))return;post('ui_click','navigation',{label:text,path:location.pathname})},{passive:true})}catch{}}function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}init()})();
