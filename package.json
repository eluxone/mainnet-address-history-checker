{
  "name": "mainnet-address-history-checker",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/check-address.mjs && node --check api/audit-addresses.mjs && node --check api/login.mjs && node --check app.js && node --check recovery.js && node --check recovery-assistant.js && node --check login.js && node --check middleware.js",
    "dev": "vercel dev"
  }
}
